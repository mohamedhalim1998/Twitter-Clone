server:
  port: 8081

spring:
  application:
    name: "auth-service"
  rabbitmq:
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
  endpoint:
    routes:
      enabled: true
